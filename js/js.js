let items = document.querySelectorAll('#first ul:first-child li')
let contents = document.querySelectorAll('#first ul:last-child li')
let count = item.length

for(let i = 0; 1 < items.length; i++) {
    items[i].addEventListener('click', () => {

        removeActive(items)
        removeActive(contents)

        items[i].classList+
    })
}